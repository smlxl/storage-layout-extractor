//! This module contains the implementation of the [`ImplicationSet`] type for
//! the [`super::Unifier`], as well as a set of supporting types and utilities.

/// The `ImplicationSet` contains evidence items as to the type of a
/// [`Metavariable`].
#[derive(Debug)]
pub struct ImplicationSet;
