//! This module contains the implementation of the [`Metavariable`] type, as
//! well as supporting utilities.

/// The Metavariable is a representation of a semantic "variable" in the
/// symbolic execution of an EVM program.
#[derive(Debug)]
pub struct Metavariable;
